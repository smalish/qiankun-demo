{
  "name": "qiankun-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:master": "cd master && yarn install",
    "serve:master": "cd master && yarn serve",
    "serve": "vue-cli-service serve",
    "build": "vue-cli-service build",
    "lint": "vue-cli-service lint"
  }
}
